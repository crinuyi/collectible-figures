@model IEnumerable<collectible_figures.Models.Series>

@{
    ViewBag.Title = "Lista serii";
}

<div class="panel panel-warning">
    <div class="panel-heading">
        <h3 class="panel-title">Lista serii</h3>
    </div>
    <div class="panel-body">
        @*panel content*@
        @if (User.Identity.IsAuthenticated) {
            <p>
                <div>
                    <button type="button"
                            class="btn btn-default"
                            onclick="location.href='@Url.Action("Create", "Series")'">
                        Dodaj nowy typ
                    </button>
                </div>
            </p>
        }

        <table class="table">
            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @if (Request.IsAuthenticated) {
                            <button type="button"
                                    class="btn btn-warning btn-primary btn-xs"
                                    onclick="location.href='@Url.Action("Edit", "Series", new { @id = item.SeriesID })'">
                                Edytuj
                            </button>
                            <button type="button"
                                    class="btn btn-danger btn-primary btn-xs"
                                    onclick="location.href='@Url.Action("Delete", "Series", new { @id = item.SeriesID })'">
                                Usuń
                            </button>
                        }
                        <button type="button"
                                class="btn btn-info btn-primary btn-xs"
                                onclick="location.href='@Url.Action("Details", "Series", new { @id = item.SeriesID })'">
                            Szczegóły
                        </button>
                    </td>
                </tr>
            }
        </table>
    </div>
</div>